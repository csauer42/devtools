Additional Steps for Setup:

(for dual boot w/ Windows)
Add RealTimeIsUniversal (DWORD32) to HKEY_LOCAL_MACHINE\System\CurrentControlSet\Control\TimeZoneInformation in WinReg

(Ubuntu)
Enable Canonical Partner Repo
Fix dock (bottom/hide/both monitors)

Fix power settings to prevent sleep

Install vpn/update hosts

Configure minicom
